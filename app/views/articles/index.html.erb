<% provide(:title, 'お便り一覧') %>

<h2><i class="far fa-file-alt"></i>&nbsp;お便り一覧</h2>

<div class="container">
  <div class="article container">
    <div class="article__group">

      <div class="article__new">
        <% if current_user.admin? %>
          <div>
            <%= link_to '新規登録', new_article_path, class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>

      <table class="table table-striped">
        <thead>
          <tr>
            <th><%= "#{Article.human_attribute_name :issue_date}" %></th>
            <th>添付ファイル</th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @articles.each do |article| %>
            <tr>
              <td><%= link_to article.issue_date.strftime("%Y年%m月%d日"), article_path(article) %></td>
              <td><%= link_to article.letter.filename, rails_blob_path(article.letter), target: "_blank", rel: "noopener noreferrer" %></td>
              <% if current_user.admin? %>
                <td><%= link_to '編集', edit_article_path(article), class: "btn btn-success" %></td>
                <td><%= link_to '削除', article_path(article), class: "btn btn-danger",
                                                          method: :delete,
                                                          data: { confirm: "#{article.letter.filename}を削除してよろしいですか？" } %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>

     <!--
      <ul class="article__year_list">
        <li>
        <% @article_years.each do |issue_date, article| %>
          <span><%= issue_date %></span>
        <% end %>
        </li>
      </ul>
      
      <% @articles.each do |article| %>
        <ul class="article__list">
          <li>
            <span><%= article.issue_date.strftime("%Y年%m月%d日") %></span>
            <span><%= link_to article.letter.filename, rails_blob_path(article.letter), target: "_blank", rel: "noopener noreferrer" %></span>
            <span class="article__edit"><%= link_to '編集', edit_article_path(article) %></span>
            <span class="article__delete">
              <%= link_to '削除', article_path(article), method: :delete,
                                                        data: { confirm: "#{article.letter.filename}を削除してよろしいですか？" } %>
            </span>
          </li>
        </ul>
      <% end %>
      -->
    </div>
  </div>
</div>
