$("#edit-gallery").html("<%= escape_javascript(render 'edit') %>");
$("#edit-gallery").modal("show");

$('#edit-gallery').on('shown.bs.modal', function() {
    $('#modal-edit-foucus').focus();
});

$('form').submit(function() {
  $("#submit-<%= modal_form_class %>-gallery").click();
});
